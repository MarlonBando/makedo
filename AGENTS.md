Before implementing feature run the tests and after check the test again. 
Add tests for new feature

When writing code use go idiomatic code, pre allocate where you can and minimize GC pressure. Re-use code where possible.
